CREATE TABLE tx_bitconexteam_domain_model_member (
	name varchar(255) NOT NULL DEFAULT '',
	lastname varchar(255) NOT NULL DEFAULT ''
);

CREATE TABLE tx_bitconexteam_domain_model_member (
	categories int(11) unsigned DEFAULT '0' NOT NULL
);
